{
  "name": "chimpions-attribute-arena",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ui": "node scripts/build-ui-assets.js",
    "postinstall": "npm run build:ui",
    "predev": "npm run build:ui",
    "dev": "node server/index.js",
    "prestart": "npm run build:ui",
    "start": "node server/index.js",
    "test": "node --test",
    "import": "node scripts/import-chimpions.js",
    "validate:collection": "node scripts/validate-collection.js",
    "validate:collection:strict": "node scripts/validate-collection.js --strict",
    "validate:ui": "node scripts/validate-ui-assets.js",
    "balance": "node scripts/balance-report.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
